import './globals.css';

export const metadata = {
  title: 'NEXUS AI — Turn ambition into action',
  description: 'Transform any goal into a visual, actionable roadmap.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
