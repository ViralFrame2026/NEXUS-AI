const $=s=>document.querySelector(s),goal=$('#goal'),generate=$('#generate'),workspace=$('#workspace'),mission=$('#mission'),output=$('#output'),insight=$('#insight'),counter=$('#counter'),progress=$('#progress'),bar=$('#bar'),missionType=$('#missionType'),horizon=$('#horizon'),nextMove=$('#nextMove');const esc=s=>s.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));goal.oninput=()=>counter.textContent=`${goal.value.length} / 240`;document.querySelectorAll('[data-goal]').forEach(b=>b.onclick=()=>{goal.value=b.dataset.goal;goal.oninput();goal.focus()});function analyze(text){const t=text.toLowerCase(),num=(t.match(/(\d+)\s*(day|days|día|días|week|weeks|semana|semanas|month|months|mes|meses)/)||[]);let type='PROJECT',timeline=num.length?`${num[1]} ${num[2]}`:'Adaptive';if(/learn|study|aprender|estudiar|course|curso/.test(t))type='LEARNING';else if(/launch|business|brand|saas|startup|tienda|negocio|marca|lanzar|sell|vender/.test(t))type='VENTURE';else if(/job|career|trabajo|empleo|cv|linkedin|interview/.test(t))type='CAREER';else if(/fitness|gym|weight|entren|peso|muscle|correr/.test(t))type='FITNESS';return{type,timeline}}
const plans={LEARNING:[['Define mastery','Turn the subject into one measurable capability and define proof that demonstrates it.','Avoid vague goals: choose what you will be able to do, not what you will read.'],['Map the 20%','Identify the small set of concepts that unlock most practical progress.','Cut low-value theory until the core is usable.'],['Build a feedback loop','Alternate short learning blocks with immediate practice and correction.','Feedback speed matters more than study volume.'],['Ship proof','Create one real artifact that forces the skill into practical use.','A finished project exposes gaps faster than another tutorial.'],['Close the gaps','Review failures, target weak areas and repeat with higher difficulty.','Improve from evidence, not intuition.']],VENTURE:[['Define the wedge','Choose one audience, one painful problem and one outcome worth paying for.','Narrow beats broad at the beginning.'],['Validate demand','Test the riskiest assumption with real people before expanding scope.','Seek behavior and commitments, not compliments.'],['Build the core','Create the smallest end-to-end experience that delivers the promised value.','Protect the core outcome from feature creep.'],['Launch deliberately','Reach a focused first audience and observe where real behavior differs from assumptions.','Instrument feedback before adding scale.'],['Compound signal','Keep what users value, remove friction and turn evidence into the next iteration.','Scale learning before scaling complexity.']],CAREER:[['Position','Define the target role and the evidence that makes your profile relevant.','One clear positioning is stronger than a generic profile.'],['Package proof','Align CV, portfolio and profile around outcomes, skills and demonstrable work.','Show evidence before adjectives.'],['Build a target list','Prioritize high-fit opportunities instead of mass applying blindly.','Fit improves both response rate and interview quality.'],['Reach out','Use tailored applications and concise direct outreach with a clear reason for fit.','Specificity signals effort and relevance.'],['Optimize the funnel','Track applications, replies and interviews to find the weakest conversion point.','Improve the stage that is actually blocking you.']],FITNESS:[['Set the baseline','Define the measurable target, current baseline and sustainable constraints.','Use trends rather than single-day measurements.'],['Build consistency','Create a repeatable training, nutrition and recovery rhythm.','Consistency precedes optimization.'],['Progress deliberately','Increase challenge gradually while protecting technique and recovery.','More is useful only when recovery can support it.'],['Measure signals','Track a small set of useful indicators tied to the goal.','Avoid reacting to noisy daily changes.'],['Adjust one variable','Review trends and modify one lever at a time.','Controlled changes make results easier to understand.']],PROJECT:[['Define the finish line','Convert the ambition into one measurable outcome with a clear definition of done.','Clarity prevents invisible scope growth.'],['De-risk first','Identify the largest unknown and run the cheapest useful test.','Solve uncertainty before polishing execution.'],['Build the proof','Create the smallest meaningful version that demonstrates the core idea.','A working proof creates better information than planning alone.'],['Deliver to reality','Put the result in front of the people it is intended to help.','Real usage reveals what internal review cannot.'],['Evolve from evidence','Measure results and use the strongest signal to choose the next iteration.','Do not add complexity without a reason.']]};function build(text){const a=analyze(text);return{context:a,steps:plans[a.type]}}
function persist(text,data,done){localStorage.setItem('nexusMission',JSON.stringify({text,data,done}))}function updateProgress(){const checks=[...document.querySelectorAll('.check')],done=checks.filter(x=>x.checked).length,pct=Math.round(done/checks.length*100)||0;progress.textContent=pct+'%';bar.style.width=pct+'%';const first=checks.find(x=>!x.checked);nextMove.textContent=first?first.closest('.card').querySelector('h3').textContent:'Mission complete — review results and define the next iteration.';return checks.filter(x=>x.checked).map(x=>+x.dataset.i)}
function render(data,saved=[]){missionType.textContent=data.context.type;horizon.textContent=data.context.timeline.toUpperCase();output.innerHTML='<div class="roadmap">'+data.steps.map((x,i)=>`<article><div class="node"><span>${String(i+1).padStart(2,'0')}</span></div><div class="card ${saved.includes(i)?'done':''}"><div class="meta"><span>${i===0?'START HERE':'PHASE '+(i+1)}</span><b>${i<2?'CRITICAL':'FOCUS'}</b></div><h3>${esc(x[0])}</h3><p>${esc(x[1])}</p><div class="signal"><span>WHY IT MATTERS</span>${esc(x[2])}</div><label><input class="check" data-i="${i}" type="checkbox" ${saved.includes(i)?'checked':''}><span>COMPLETE PHASE</span></label></div></article>`).join('')+'</div>';document.querySelectorAll('.check').forEach(c=>c.onchange=()=>{c.closest('.card').classList.toggle('done',c.checked);const done=updateProgress();persist(mission.textContent,data,done)});updateProgress();insight.innerHTML='<span>✦ NEXUS STRATEGY</span><h3>Reduce uncertainty before increasing effort.</h3><p>The first two phases carry the most leverage. Finish them before expanding scope; your progress is stored on this device automatically.</p>';insight.classList.remove('hidden')}
function run(){const text=goal.value.trim();if(text.length<5)return;mission.textContent=text;workspace.classList.remove('hidden');insight.classList.add('hidden');generate.disabled=true;generate.textContent='ANALYZING MISSION…';output.innerHTML='<div class="scan"><div class="scan-copy"><b>DECOMPOSING OBJECTIVE</b><p>Extracting intent · detecting horizon · ranking execution risk</p></div></div>';workspace.scrollIntoView({behavior:'smooth',block:'start'});setTimeout(()=>{const data=build(text);persist(text,data,[]);render(data);generate.disabled=false;generate.textContent='BUILD MY ROADMAP →'},1050)}generate.onclick=run;goal.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key==='Enter')run()});$('#reset').onclick=()=>{localStorage.removeItem('nexusMission');workspace.classList.add('hidden');goal.value='';goal.oninput();goal.focus();scrollTo({top:0,behavior:'smooth'})};try{const s=JSON.parse(localStorage.getItem('nexusMission')||'null');if(s?.text&&s?.data){goal.value=s.text;goal.oninput();mission.textContent=s.text;workspace.classList.remove('hidden');render(s.data,s.done||[])}}catch(e){}